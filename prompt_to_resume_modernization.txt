**Role**: You are the next modernization agent for the SV-UVM Guide backlog.

**Execution Flow**
`prompt_to_resume_modernization.txt` -> `TASKS.md` -> optional supporting context files

**Instructions for the Agent:**
1. Read `TASKS.md` first. It is the only live tracker for status, priority, dependencies, validation requirements, and handoff rules.
2. In `TASKS.md`, use `Active Backlog Summary` to find the first task whose status is `todo` and whose dependencies are already satisfied.
3. Read that task's full implementation brief in `Detailed Task Briefs` before editing anything.
4. If you encounter legacy task IDs such as `CURR-*` or `AUD-*`, use the `Canonical Task Crosswalk` in `TASKS.md`. Do not revive old tracking systems.
5. Use `comprehensive_lrm_audit_report.md`, `curriculum_modernization_tasks.md`, and `curriculum_audit_findings.md` only for deeper rationale, audit evidence, and older acceptance criteria. They are not live trackers.
6. Execute only the selected task. Do not silently bundle unrelated backlog work into the same pass.
7. Before marking the task complete, run the required validation from `TASKS.md`. At minimum this usually includes:
   - `npx tsx scripts/generate-curriculum-data.ts`
   - `npm run lint`
   - `npx vitest --run`
   - targeted Playwright coverage for learner-facing route or interactive changes
8. Update status only in `TASKS.md`. Do not edit status fields in the supporting planning documents.
9. Commit or push only if the user explicitly asks for it in that session.
10. When you yield, tell the user to reuse this exact file (`prompt_to_resume_modernization.txt`) in the next session.
