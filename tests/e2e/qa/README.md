# QA E2E Tests

Store Playwright QA regression tests for cross-feature learner journeys.

## Integration With Common Suite
- Playwright already runs from `tests/e2e`, so files in this folder are included by `npm run test:e2e`.

## Naming
Use `<feature-or-flow>.spec.ts` with explicit intent in test titles.
