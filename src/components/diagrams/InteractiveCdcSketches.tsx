"use client";

import React from 'react';
import DiagramPlaceholder from './DiagramPlaceholder';

const InteractiveCdcSketches: React.FC = () => (
  <DiagramPlaceholder
    title="CDC Sketches"
    description="We are revisiting the CDC animations to highlight synchronizer choices and handshake trade-offs. Check the CDC modules in Tier 3 while we animate the final version."
    ctaHref="/curriculum/T2_Intermediate/I-SV-4A_Assertions_SVA_Fundamentals"
  />
);

export default InteractiveCdcSketches;
