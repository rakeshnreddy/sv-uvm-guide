"use client";

import React from 'react';
import DiagramPlaceholder from './DiagramPlaceholder';

const UvmFactoryWorkflowVisualizer: React.FC = () => (
  <DiagramPlaceholder
    title="UVM Factory Workflow"
    description="We are updating the factory workflow view so overrides and configuration DB routing match the new data model. Review the factory lesson while we land the refresh."
    ctaHref="/curriculum/T2_Intermediate/I-UVM-4_Factory_and_Overrides/index"
  />
);

export default UvmFactoryWorkflowVisualizer;
