**Role**: You are an autonomous coding agent continuing work on the sv-uvm-guide repository.

**Context & Workflow Instructions:**
This is a multi-session project. You must strictly track and dictate state via `TASKS.md` to prevent context loss or double-work when this session inevitably closes due to context length/rate limits.

**Last Session Summary:**
Completed tasks `INFRA-7-PROTOCOL-WAVEFORM-FOUNDATION` and `CONTENT-PROT-1-AMBA-FOUNDATIONS`.
Added `ProtocolWaveform` shared WaveDrom component, `AmbaFamilyExplorer` visualizer, and authored `B-AMBA-1_Protocol_Families_and_Tradeoffs` module. Updated tests and curriculum data.

**Next Up in Backlog:**
Task 34: `CONTENT-AHB-1-DESIGN-TIMING` (P0)
Task 35: `VIZ-11-AHB-PIPELINE-BURST` (P0)

**Step 1: On-board to Current State**
1. Read `TASKS.md` entirely. It is your single source of truth.
2. Under "Active Backlog Summary", look for the top-most row where `Status` is `todo` and all `Depends On` tasks are marked `complete`.
3. If no tasks are `todo`, the epoch is finished.

**Step 2: Execution Rules**
1. Navigate to the Detailed Task Brief for your selected `todo` task inside `TASKS.md`.
2. Do not combine tasks. Focus exclusively on the checklist for your single selected task to preserve the context window.
3. Write robust, modular code. If building an interactive component (`src/components/curriculum/interactives/` or `src/components/visualizers/`), remember to:
   - Make it a `"use client"` component.
   - Register it properly in `src/app/curriculum/[...slug]/page.tsx` using `next/dynamic` with `ssr: false`.
   - Write React Testing Library unit tests in `tests/components/`.

**Step 3: Validation Protocol**
Before marking your task `complete`, you MUST run:
1. `npm run generate:curriculum` (if MDX, curriculum data, or lab registry was updated)
2. `npm run lint`
3. `npx vitest --run`
4. Review Playwright E2E suites (`tests/e2e/`) if you edited core navigation/routing.

**Important:** Node.js is at `/opt/homebrew/bin/node` — always prefix commands with `export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH" &&` to ensure npm/npx are available.

**Step 4: Handoff Protocol (When Context is Full or Task is Complete)**
1. Check off your executed deliverables in the Detailed Task Brief section of `TASKS.md`.
2. ONLY if all deliverables and tests are met, change the Task Status to `complete` in BOTH the Detailed Brief AND the top Active Backlog Summary table.
3. `git add .`, `git commit -m "feat(TASK_ID): completed work"`, `git push`.
4. If your context window is filling up or you have been executing for more than 15-20 actions:
   - Call the user notification tool and pass the session back cleanly.
   - Tell the user to start a new chat session and paste THIS exact file (`SESSION_HANDOFF.txt`) back in along with the standard "read and continue" command.

**DO NOT DEVIATE from TASKS.md.**

Start your work loop by analyzing `TASKS.md` now!
