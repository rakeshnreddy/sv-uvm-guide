# Session Handoff: AMBA Protocol Curriculum Expansion

## What Was Accomplished
1. **Curriculum Re-architecture:** We massively expanded the AHB/AXI curriculum plan in `docs/planning/ahb-axi-protocol-guide-plan.md` to a 13-module structure, covering beginner-to-expert topics including intuition/memory hooks, pitfalls/deadlocks, expert features (QoS, Atomics), and future protocols (ACE/CHI).
2. **Backlog Overhaul:** We updated `TASKS.md` to map Tasks 34-60 to this new expanded curriculum, including definitions for four new visualizers (`ProtocolAnalogyExplorer`, `AxiDeadlockSimulator`, `AxiMemoryMathVisualizer`, `ExclusiveAccessVisualizer`) and new debug labs.
3. **Module Authoring:** We completed Task 34 (`CONTENT-PROT-2-INTUITION`), creating the `B-AMBA-2_Protocol_Intuition_and_Memory_Hooks` MDX module. This module uses "Shared Highway" and "Mail Delivery System" analogies to build a permanent mental model of AHB and AXI channels.
4. **Task Updates:** We marked Task 34 as `complete` in `TASKS.md`.

## Next Steps for New Agent Session
1. **Start with Task 35 (`VIZ-11-PROTOCOL-ANALOGY`):** Your primary objective is to build the interactive `ProtocolAnalogyExplorer` React component. 
   - This component should visually represent the "Mail Delivery System" analogy for AXI channels as described in the `B-AMBA-2` module.
   - It needs to show Master $\rightarrow$ Slave writes (sending a package + receiving a receipt via the B channel) and reads (ordering a package + receiving it via the R channel).
2. **Integration:** Embed the new visualizer into `content/curriculum/T3_Advanced/B-AMBA-2_Protocol_Intuition_and_Memory_Hooks/index.mdx` using the `<InteractiveWrapper>` block currently holding a comment.
3. **Registration:** Register the visualizer in `src/app/curriculum/[...slug]/page.tsx`.
4. **Validation:** Write unit tests for the visualizer in `tests/components/ProtocolAnalogyExplorer.test.tsx` using Vitest and React Testing Library.

## Important Context
- **Single Source of Truth:** `TASKS.md` dictates the flow. Always verify the next `todo` item there.
- **Agent Guidelines:** Adhere to the rules in `AGENTS.md` and the user guidelines. 
- **Tooling:** Remember to run `npm run generate:curriculum` and `npm run build` after adding new MDX content or components to ensure Next.js routing picks them up.
